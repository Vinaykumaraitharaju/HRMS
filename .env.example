APP_NAME=Wavelynk
ENVIRONMENT=local
# Use Render Postgres in production. Render's postgres:// URL is supported automatically.
DATABASE_URL=postgresql+asyncpg://postgres:postgres@localhost:5432/wavelynk
REDIS_URL=redis://localhost:6379/0
JWT_SECRET_KEY=change-me-in-production
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=60
ADMIN_EMAIL=vinaysiri2000@gmail.com
ADMIN_PASSWORD=TempAdmin#2026
OFFICE_LATITUDE=12.9716
OFFICE_LONGITUDE=77.5946
OFFICE_RADIUS_METERS=250

# Recommended for Render email/OTP because outbound SMTP can be blocked.
RESEND_API_KEY=re_xxxxxxxxxxxxxxxxxxxxxxxxx
RESEND_FROM_EMAIL=HRMS <onboarding@resend.dev>
# Local-only fallback: prints OTP in server logs when no email provider is configured.
ALLOW_DEV_OTP=false
